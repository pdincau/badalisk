BADALISK vsn 1.0

A proxy server written in Erlang

To install use:

$ make

To run use:

$ erl -boot badalisk_rel-1

In case of problems compliling try:

$ erl -pa ./ebin
1> systools:make_script("badalisk_rel-1", [local]).
ok
